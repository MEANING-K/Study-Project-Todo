/* eslint-disable prettier/prettier */
// sagas/rootSaga.js

import { all } from 'redux-saga/effects';

// 여기에 모든 Saga들을 묶어주는 rootSaga를 정의합니다.
export default function* rootSaga() {
  yield all([]);
}
